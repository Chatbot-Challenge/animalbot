

FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY chatbot/ .
RUN useradd -m dev && mkdir -p /app/data && chown -R dev:dev /app
USER dev
ENV PYTHONUNBUFFERED=1
EXPOSE 8000
CMD ["python", "api.py"]